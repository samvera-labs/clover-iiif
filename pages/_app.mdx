import { Theme } from "@radix-ui/themes";

export default function CloverDocsApp({ Component, pageProps }) {
  return (
    <Theme accentColor="ruby" radius="none">
      <Component {...pageProps} />
    </Theme>

);
}
