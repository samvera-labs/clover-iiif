import { style } from "@vanilla-extract/css";

export const iconBase = style({
  display: "inline-flex",
});

export const iconSizeLarge = style({
  height: "4rem",
  width: "4rem",
});

export const iconSizeMedium = style({
  height: "2rem",
  width: "2rem",
});

export const iconSizeSmall = style({
  height: "1rem",
  width: "1rem",
});
