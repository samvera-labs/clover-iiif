import { globalCss } from "@stitches/react";

const globalCloverImage = globalCss({
  ".clover-theme--image": { width: "100%", height: "100%" },
});

const globalCloverViewer = globalCss({});

export { globalCloverImage, globalCloverViewer };
